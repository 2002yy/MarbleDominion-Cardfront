BallWar v1.9.8

本次更新：
1. 视口调整为 1120×720。
2. 恢复子弹上限从 300 提高到 600：
   - MAX_RESTORE_BULLETS = 600。
3. 60×60 下暂停/退出按钮改为右侧纵向排列，并向内收：
   - 暂停按钮 y=86；
   - 退出按钮 y=124；
   - 避免右侧退出按钮在编辑器预览或窄显示区域被裁掉。
4. 存档版本兼容判断保留，并增加用户可见提示：
   - 存档损坏；
   - 存档版本不兼容；
   - 存档结构不完整。
5. 暂停时停止游戏内 +球 按钮呼吸动画，避免暂停后游戏内 UI 仍像在运行。
